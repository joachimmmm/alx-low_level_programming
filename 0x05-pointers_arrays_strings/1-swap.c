#include "main.h"
#include <stdio.h>

/**
 * swap_int - swaps the values of 2 ints.
 * @a: the first int
 * @b: the second int
 * Return: nothing
 */
void swap_int(int *a, int *b)
{
	int temp = *a;
	*a = *b;
	*b = temp;#include "main.h" 
}
