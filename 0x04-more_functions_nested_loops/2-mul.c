#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies 2 numbers
 * @a: the first value
 * @b: the second value
 * Return: always 0
 */

int mul(int a, int b)
{
	int multiply;
	multiply = (a * b);
	return (multiply);
}
